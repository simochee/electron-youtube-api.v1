<!DOCTYPE html>
html(lang="ja")
head
    meta(charset="UTF-8")
    title Document
body

    app

    script.
        require('electron-connect').client.create()
    
    script(src="../js/bundle.min.js")